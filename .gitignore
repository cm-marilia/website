# Planilhas e dados brutos NÃO vão para este repositório público
# (contêm/podem conter dados pessoais — CPF — em texto puro).
# Ficam apenas localmente ou em repositório privado.
/planilhas/
/dados/
